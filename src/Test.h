#pragma once
#include <vector>

// Generate all the symbols bit strings upto and including length n
std::vector<std::vector<bool>> generate_bitstrings(uint length);

extern const std::vector<double> probabilities;
